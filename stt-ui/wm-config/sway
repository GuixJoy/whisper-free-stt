# STT Widget — Sway Configuration
# Add these lines to your ~/.config/sway/config

# Float the widget window and make it sticky (visible on all workspaces)
for_window [app_id="stt-widget"] floating enable, sticky enable, border pixel 0
